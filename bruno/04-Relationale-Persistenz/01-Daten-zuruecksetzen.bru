meta {
  name: Daten zuruecksetzen
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/reset
  body: none
  auth: none
}

tests {
  test("Seed-Daten wurden fuer Persistenztests zurueckgesetzt", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().ok).to.equal(true);
  });
}
